import type {NextPage} from 'next'
import styled from 'styled-components';

const Home: NextPage = () => {
  return (
    <Main>Test</Main>
  )
}

const Main = styled.main`
  color: red;
`;

export default Home
